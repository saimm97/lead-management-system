# LeadPro backend — pip install -r requirements.txt
# Web framework / server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Database (async driver used by the app; psycopg2 is a fallback for sync tools/migrations)
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
psycopg2-binary>=2.9.9
alembic>=1.14.0

# Validation / settings
pydantic[email]>=2.10.0
pydantic-settings>=2.6.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0,<4.1.0

# Background tasks
celery[redis]>=5.4.0
redis>=5.2.0

# HTTP / uploads / templating
httpx>=0.28.0
python-multipart>=0.0.17
jinja2>=3.1.4

# Excel / documents (import + CV parsing)
openpyxl>=3.1.5
pypdf>=4.0.0
python-docx>=1.1.0

# Google Calendar integration
google-api-python-client>=2.0.0
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
